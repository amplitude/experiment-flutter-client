import Flutter
import UIKit
import XCTest

@testable import amplitude_experiment

// Main tests: AmplitudeExperimentPluginTests, ExperimentSdkCodecTest, TestDataHelpers

class RunnerTests: XCTestCase {
  // Placeholder test - main tests are in separate files
  func testPlaceholder() {
    XCTAssertTrue(true)
  }
}
